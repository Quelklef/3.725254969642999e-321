{

  get/char
  put/char

  push nan/x'0 swap
  -- The following tests if the read character was \0 or not
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  neg rotr [ swap [ neg ] neg swap neg ]
  drop

[ drop } ] stop
